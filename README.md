# 🖥️ Open Ahrefs Admin

Веб-интерфейс для управления экосистемой **Open Ahrefs**, построенный на **FastAPI**. Служит панелью мониторинга результатов краулинга и точкой входа для управления задачами в Redis.

## 🚀 Основные возможности

* **Управление очередью**: Форма добавления URL напрямую в очередь Redis (`crawler:start_urls`) для запуска задач.
* **Просмотр данных**: Отображение проиндексированных страниц из PostgreSQL с поддержкой пагинации.
* **Интерфейс на DaisyUI**: Лаконичный дизайн с использованием Tailwind CSS компонентов для удобной работы с табличными данными.

## 🏗️ Архитектура

Проект интегрирован в общую сеть краулера:

1. **Backend**: FastAPI обрабатывает входящие запросы и координирует работу с БД и Redis.
2. **Database Layer**: Использование асинхронных сессий SQLAlchemy 2.0.
3. **DI (Dependency Injection)**: Логика работы с базой данных реализована через современные конструкции FastAPI (`Depends`), что обеспечивает чистоту кода и эффективное управление жизненным циклом сессий.
4. **Integration**: Прямое взаимодействие с очередью `crawler:start_urls` для передачи задач распределенным паукам Scrapy.

## 🛠️ Технологический стек

* **Core:** FastAPI
* **Database:** PostgreSQL, SQLAlchemy 2.0 (Asyncpg)
* **Storage:** Redis
* **Frontend:** Jinja2, Tailwind CSS, DaisyUI

## 📦 Быстрый запуск

Запуск проекта полностью контейнеризирован. Docker-образ админки автоматически подготовит среду и поднимет базу данных PostgreSQL.

### Подготовка

Перед запуском убедитесь, что в вашей сети доступен инстанс **Redis** (именно через него админка общается с краулером).

### Запуск через Docker Compose

```bash
docker-compose up --build
```

После успешного запуска панель будет доступна по адресу: `http://localhost:8000`

## ⚙️ Конфигурация (.env)

Параметры подключения передаются через переменные окружения:

- `DB_USER`
- `DB_PASS`
- `DB_NAME` (имя базы данных)
- `DB_HOST`
- `DB_PORT`
- `REDIS_HOST`
- `REDIS_PORT`
